(()=>{
const STYLE=document.createElement('style');STYLE.textContent=`body.pshBooting #loginView,body.pshBooting #appView{visibility:hidden!important}#pshBoot{position:fixed;inset:0;z-index:9999;display:none;place-items:center;background:linear-gradient(135deg,#fffaf6,#fff2e7);color:#8a2d0c;font-family:Inter,system-ui,-apple-system,"Segoe UI",Arial,sans-serif}body.pshBooting #pshBoot{display:grid}.pshBootCard{display:grid;place-items:center;gap:12px}.pshBootHeart{font-size:38px}.pshBootText{font-weight:850}`;document.head.appendChild(STYLE);
const boot=document.createElement('div');boot.id='pshBoot';boot.innerHTML='<div class="pshBootCard"><div class="pshBootHeart">♥</div><div class="pshBootText">Patient Service Hub</div></div>';document.body.appendChild(boot);document.body.classList.add('pshBooting');
const KEY='psh_nav_state_v3';
function read(){try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}}
function restoreDrawer(st){['cath','echo'].forEach(id=>{const el=document.getElementById(id);const k=id+'Collapsed';if(el&&typeof st[k]==='boolean')el.classList.toggle('hidden',st[k])})}
async function restoreSavedState(){const st=read();restoreDrawer(st);if(st.page==='service'&&st.service&&typeof openService==='function'){await openService(st.service);setTimeout(()=>{const d=document.getElementById('serviceDate'),s=document.getElementById('serviceStatus'),rf=document.getElementById('rangeFrom'),rt=document.getElementById('rangeTo');if(d&&st.date!==undefined)d.value=st.date||'';if(s&&st.status!==undefined)s.value=st.status||'';if(rf&&st.rangeFrom!==undefined)rf.value=st.rangeFrom||'';if(rt&&st.rangeTo!==undefined)rt.value=st.rangeTo||'';if(typeof loadService==='function')loadService()},30);return}if(st.page&&st.page!=='dashboard'&&document.getElementById(st.page)&&typeof show==='function')show(st.page)}
async function finish(){const {data:{session}}=await sb.auth.getSession();if(!session){document.getElementById('loginView')?.classList.remove('hidden');document.getElementById('appView')?.classList.add('hidden');document.body.classList.remove('pshBooting');return}let n=0;const timer=setInterval(async()=>{n++;if(typeof profile!=='undefined'&&profile&&document.getElementById('appView')&&!document.getElementById('appView').classList.contains('hidden')){clearInterval(timer);await restoreSavedState();requestAnimationFrame(()=>requestAnimationFrame(()=>document.body.classList.remove('pshBooting')))}else if(n>120){clearInterval(timer);document.body.classList.remove('pshBooting')}},50)}
finish();
})();