import{sb}from'./supabase.js';
async function login(form,observer){const msg=form.querySelector('.msg'),btn=form.querySelector('button'),fd=new FormData(form);msg.textContent='';btn.disabled=true;const{data,error}=await sb.auth.signInWithPassword({email:fd.get('email').trim().toLowerCase(),password:fd.get('password')});if(error){msg.textContent=error.message;btn.disabled=false;return}const{data:p}=await sb.from('profiles').select('role,is_active').eq('id',data.user.id).single();if(!p?.is_active||observer!== (p?.role==='observer')){await sb.auth.signOut();msg.textContent=!p?.is_active?'Account inactive. Contact the owner.':observer?'Use the main sign in for this account.':'Observers must use Observer access.';btn.disabled=false;return}location.replace('app.html#dashboard')}
document.querySelector('#mainLogin').onsubmit=e=>{e.preventDefault();login(e.currentTarget,false)};document.querySelector('#observerLogin').onsubmit=e=>{e.preventDefault();login(e.currentTarget,true)};
