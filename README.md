# Resident Training v1.0.57

Frontend-only visual update.

- Enlarges the current residency year prominently on the resident dashboard.
- Removes the small sentence below the residency year.
- Applies consistently to Years 1–5.
- No Supabase SQL changes are required.

Replace `app.html`, `index.html`, `assets/app.js`, and `assets/style.css`, then hard refresh.
